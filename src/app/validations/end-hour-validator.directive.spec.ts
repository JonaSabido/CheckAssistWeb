import { EndHourValidatorDirective } from './end-hour-validator.directive';

describe('EndHourValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EndHourValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
