import { DateMeetingValidatorDirective } from './date-meeting-validator.directive';

describe('DateMeetingValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DateMeetingValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
