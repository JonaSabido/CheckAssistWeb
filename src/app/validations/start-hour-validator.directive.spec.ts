import { StartHourValidatorDirective } from './start-hour-validator.directive';

describe('StartHourValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StartHourValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
