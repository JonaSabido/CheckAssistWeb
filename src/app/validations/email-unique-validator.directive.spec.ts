import { EmailUniqueValidatorDirective } from './email-unique-validator.directive';

describe('EmailUniqueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailUniqueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
